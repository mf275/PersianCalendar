jdk: openjdk17

install:
  - ./gradlew :lib:publishToMavenLocal :android:publishToMavenLocal

# Explicit module mapping for Jitpack
jitpack:
  artifact:
    - "lib/build/publications/mavenJava/pom-default.xml"
    - "android/build/publications/release/pom-default.xml"
  artifactId:
    - "persian-calendar"
    - "persian-calendar-android"